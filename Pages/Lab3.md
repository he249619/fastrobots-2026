Notes:

1. JST wire connection is backwards (red = power, black = ground, blue = sda, yellow = scl)
2. I think that I will try to change the address of the ToF sensors upon boot up in the void setup(). If I can't get that to work then I will use the Xshut pins
3. 
